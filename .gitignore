# Go
server/bin/
*.test

# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Wrangler
.wrangler/
.dev.vars
.dev.vars.*

# EAS / native builds
app/ios/
app/android/
*.ipa
*.apk
*.aab

# Env & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Local-only Claude working notes (specs, plans, task reports/diffs) —
# not meant for every repo collaborator to see.
