// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: compliance.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const blockUser = `-- name: BlockUser :exec
INSERT INTO blocked_users (blocker_id, blocked_id)
VALUES ($1, $2)
ON CONFLICT DO NOTHING
`

type BlockUserParams struct {
	BlockerID uuid.UUID `json:"blocker_id"`
	BlockedID uuid.UUID `json:"blocked_id"`
}

func (q *Queries) BlockUser(ctx context.Context, arg BlockUserParams) error {
	_, err := q.db.Exec(ctx, blockUser, arg.BlockerID, arg.BlockedID)
	return err
}

const deleteUser = `-- name: DeleteUser :execrows
DELETE FROM users WHERE id = $1
`

func (q *Queries) DeleteUser(ctx context.Context, id uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteUser, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const detachUserFromCourts = `-- name: DetachUserFromCourts :exec

UPDATE courts SET submitted_by = NULL WHERE submitted_by = $1
`

// Account deletion and user blocking (App Store 5.1.1(v) / UGC policies).
// Courts outlive their submitter; flags keep their resolution but lose the
// resolver attribution. Everything else cascades from the users row.
func (q *Queries) DetachUserFromCourts(ctx context.Context, submittedBy *uuid.UUID) error {
	_, err := q.db.Exec(ctx, detachUserFromCourts, submittedBy)
	return err
}

const detachUserFromResolvedFlags = `-- name: DetachUserFromResolvedFlags :exec
UPDATE flags SET resolved_by = NULL WHERE resolved_by = $1
`

func (q *Queries) DetachUserFromResolvedFlags(ctx context.Context, resolvedBy *uuid.UUID) error {
	_, err := q.db.Exec(ctx, detachUserFromResolvedFlags, resolvedBy)
	return err
}

const listBlockedUsers = `-- name: ListBlockedUsers :many
SELECT b.blocked_id, u.display_name, b.created_at
FROM blocked_users b
JOIN users u ON u.id = b.blocked_id
WHERE b.blocker_id = $1
ORDER BY b.created_at DESC
`

type ListBlockedUsersRow struct {
	BlockedID   uuid.UUID `json:"blocked_id"`
	DisplayName string    `json:"display_name"`
	CreatedAt   time.Time `json:"created_at"`
}

func (q *Queries) ListBlockedUsers(ctx context.Context, blockerID uuid.UUID) ([]ListBlockedUsersRow, error) {
	rows, err := q.db.Query(ctx, listBlockedUsers, blockerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListBlockedUsersRow
	for rows.Next() {
		var i ListBlockedUsersRow
		if err := rows.Scan(&i.BlockedID, &i.DisplayName, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unblockUser = `-- name: UnblockUser :exec
DELETE FROM blocked_users WHERE blocker_id = $1 AND blocked_id = $2
`

type UnblockUserParams struct {
	BlockerID uuid.UUID `json:"blocker_id"`
	BlockedID uuid.UUID `json:"blocked_id"`
}

func (q *Queries) UnblockUser(ctx context.Context, arg UnblockUserParams) error {
	_, err := q.db.Exec(ctx, unblockUser, arg.BlockerID, arg.BlockedID)
	return err
}
