// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: media.sql

package gen

import (
	"context"
)

const ackObjectDeletions = `-- name: AckObjectDeletions :execrows
DELETE FROM object_deletion_queue
WHERE storage_key = ANY($1::text[])
`

func (q *Queries) AckObjectDeletions(ctx context.Context, storageKeys []string) (int64, error) {
	result, err := q.db.Exec(ctx, ackObjectDeletions, storageKeys)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const claimObjectDeletions = `-- name: ClaimObjectDeletions :many
WITH candidates AS (
    SELECT q.storage_key
    FROM object_deletion_queue q
    WHERE q.requested_at <= now()
      AND (q.claimed_at IS NULL OR q.claimed_at < now() - interval '5 minutes')
      AND NOT EXISTS (
          SELECT 1 FROM users u WHERE u.avatar_url = '/photos/' || q.storage_key
      )
      AND NOT EXISTS (
          SELECT 1 FROM court_photos p
          WHERE p.storage_key = q.storage_key AND p.status = 'visible'
      )
    ORDER BY q.requested_at
    FOR UPDATE SKIP LOCKED
    LIMIT $1
)
UPDATE object_deletion_queue q
SET claimed_at = now(), attempts = q.attempts + 1
FROM candidates c
WHERE q.storage_key = c.storage_key
RETURNING q.storage_key
`

func (q *Queries) ClaimObjectDeletions(ctx context.Context, batchSize int32) ([]string, error) {
	rows, err := q.db.Query(ctx, claimObjectDeletions, batchSize)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var storage_key string
		if err := rows.Scan(&storage_key); err != nil {
			return nil, err
		}
		items = append(items, storage_key)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const isMediaKeyAuthorized = `-- name: IsMediaKeyAuthorized :one

SELECT EXISTS (
    SELECT 1 FROM users WHERE avatar_url = '/photos/' || $1::text
    UNION ALL
    SELECT 1 FROM court_photos
    WHERE storage_key = $1 AND status = 'visible'
)::bool AS authorized
`

// Owned-media authorization and asynchronous R2 deletion queue.
func (q *Queries) IsMediaKeyAuthorized(ctx context.Context, storageKey string) (bool, error) {
	row := q.db.QueryRow(ctx, isMediaKeyAuthorized, storageKey)
	var authorized bool
	err := row.Scan(&authorized)
	return authorized, err
}

const markCourtPhotoUploaded = `-- name: MarkCourtPhotoUploaded :execrows
UPDATE court_photos
SET status = 'visible'
WHERE storage_key = $1 AND status IN ('pending', 'visible')
`

func (q *Queries) MarkCourtPhotoUploaded(ctx context.Context, storageKey string) (int64, error) {
	result, err := q.db.Exec(ctx, markCourtPhotoUploaded, storageKey)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const scheduleUploadedObjectCleanup = `-- name: ScheduleUploadedObjectCleanup :exec
INSERT INTO object_deletion_queue (storage_key, requested_at)
VALUES ($1, now() + interval '1 hour')
ON CONFLICT (storage_key) DO NOTHING
`

func (q *Queries) ScheduleUploadedObjectCleanup(ctx context.Context, storageKey string) error {
	_, err := q.db.Exec(ctx, scheduleUploadedObjectCleanup, storageKey)
	return err
}
