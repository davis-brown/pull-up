## What this changes

<!-- The behaviour that differs, and why it needed to. -->

## Verification

<!-- What you ran, and what it showed. For anything touching the map or the
     store layer, say how you checked it beyond a passing typecheck. -->

- [ ] `make test` (or `make test-int` if the store layer is involved)
- [ ] `npm --prefix app run typecheck && npm --prefix app run lint && npm --prefix app test`
- [ ] `npm --prefix deploy/api test`

## Notes

<!-- Anything deliberately left out, or that a reviewer would otherwise ask about. -->
